import { FormEvent, useState } from 'react';
import { useNavigate, useSearchParams, Link } from 'react-router-dom';
import { useAuth } from '../contexts/AuthContext';
export default function Login(){ const {signIn,profile}=useAuth(); const [params]=useSearchParams(); const navigate=useNavigate(); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [err,setErr]=useState(''); async function submit(e:FormEvent){e.preventDefault(); const r=await signIn(email,password); if(r.error){setErr(r.error);return;} const next=params.get('next'); setTimeout(()=>navigate(next || (profile?.role==='admin'?'/admin':'/')),200); } return <section className="section"><div className="container" style={{maxWidth:520}}><div className="card"><div className="card-body"><h1>Login / Đăng nhập</h1><form onSubmit={submit}><label>Email or username</label><input className="input" value={email} onChange={e=>setEmail(e.target.value)} placeholder="admin@deals68.com / hkmedi"/><label>Password</label><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><div style={{height:12}}/><button className="btn" type="submit">Login</button>{err&&<p className="notice warn">{err}</p>}</form><p className="muted">Seed examples after running seed: admin@deals68.com / deals68Admin68!, hkmedi / deals687U8.</p><Link to="/pricing">Create account</Link></div></div></div></section>}
