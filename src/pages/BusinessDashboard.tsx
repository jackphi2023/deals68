import { FormEvent, useEffect, useState, type CSSProperties } from 'react';
import { Link, Navigate, useLocation, useNavigate } from 'react-router-dom';
import { useAuth } from '../contexts/AuthContext';
import { deleteBusinessFile, deleteBusinessImage, getMyBusiness, updateBusinessImage, uploadBusinessFile, uploadBusinessImage } from '../lib/data';
import { supabase } from '../lib/supabase';
import { formatCompactMoney, percent } from '../lib/format';

type Tab = 'overview' | 'profile' | 'documents' | 'images' | 'datacenter' | 'interests' | 'requests' | 'services';
const T = (lang: 'vi' | 'en', vi: string, en: string) => lang === 'en' ? en : vi;
const tabs: { id: Tab; icon: string; vi: string; en: string; href: string }[] = [
  { id: 'overview', icon: '◎', vi: 'Tổng quan', en: 'Overview', href: '/dashboard/business' },
  { id: 'profile', icon: '✎', vi: 'Hồ sơ', en: 'Profile', href: '/dashboard/business/profile' },
  { id: 'documents', icon: '📂', vi: 'Tài liệu', en: 'Documents', href: '/dashboard/business/files' },
  { id: 'images', icon: '🖼', vi: 'Ảnh', en: 'Images', href: '/dashboard/business/images' },
  { id: 'datacenter', icon: '◆', vi: 'Data Center', en: 'Data Center', href: '/dashboard/business/financials' },
  { id: 'interests', icon: '🤝', vi: 'Nhà đầu tư', en: 'Investors', href: '/dashboard/business/investor-interest' },
  { id: 'requests', icon: '📨', vi: 'Yêu cầu data', en: 'Data requests', href: '/dashboard/business/data-requests' },
  { id: 'services', icon: '💳', vi: 'Dịch vụ & phí', en: 'Services', href: '/dashboard/business/payments' }
];
const tabMap: Record<string, Tab> = { '': 'overview', profile: 'profile', files: 'documents', documents: 'documents', images: 'images', financials: 'datacenter', valuation: 'datacenter', 'investor-interest': 'interests', interests: 'interests', proposals: 'interests', 'data-requests': 'requests', requests: 'requests', payments: 'services', plan: 'services', services: 'services' };
function resolveTab(pathname: string): Tab { const suffix = pathname.replace('/dashboard/business','').replace(/^\//,'').split('/')[0]; return tabMap[suffix] || 'overview'; }
const inputStyle: CSSProperties = { border: '1px solid #E2E8F0', borderRadius: 10, padding: '12px 13px', fontSize: 15, color: '#0F2A4A', background: '#F7FAFC', fontWeight: 500, outline: 'none', width: '100%' };
const cardStyle: CSSProperties = { background: '#fff', border: '1px solid #E7EDF3', borderRadius: 16, padding: '22px 24px' };
const labelStyle: CSSProperties = { display: 'flex', flexDirection: 'column', gap: 6 };
const labelText: CSSProperties = { fontSize: 12.5, fontWeight: 700, color: '#334155' };
function metricCard(label: string, value: string, color = '#0F2A4A') { return <div style={{ background: '#fff', border: '1px solid #E7EDF3', borderRadius: 14, padding: 18 }}><div style={{ fontSize: 11.5, fontWeight: 700, color: '#94A3B8', textTransform: 'uppercase' }}>{label}</div><div style={{ fontSize: 19, fontWeight: 800, marginTop: 6, color }}>{value}</div></div>; }
function qBand(score: number) { if (score >= 80) return { labelVi: 'Mạnh', labelEn: 'Strong', color: '#16A34A', bg: '#E9F9EF' }; if (score >= 65) return { labelVi: 'Tốt', labelEn: 'Good', color: '#1596cc', bg: '#E7F6FD' }; return { labelVi: 'Cần bổ sung', labelEn: 'Needs data', color: '#B8860B', bg: '#FEF3D3' }; }
function ext(name = '', type = '') { const e = name.includes('.') ? name.split('.').pop()?.toUpperCase() : ''; if (e) return e; if (type.includes('pdf')) return 'PDF'; if (type.includes('excel') || type.includes('spreadsheet')) return 'XLSX'; if (type.includes('word')) return 'DOCX'; if (type.includes('presentation')) return 'PPT'; return type || 'FILE'; }

export default function BusinessDashboard() {
  const { profile, loading, signOut } = useAuth();
  const navigate = useNavigate();
  const location = useLocation();
  const [lang, setLang] = useState<'vi' | 'en'>('vi');
  const [tab, setTab] = useState<Tab>(() => resolveTab(location.pathname));
  const [b, setB] = useState<any>();
  const [files, setFiles] = useState<any[]>([]);
  const [images, setImages] = useState<any[]>([]);
  const [requests, setRequests] = useState<any[]>([]);
  const [interests, setInterests] = useState<any[]>([]);
  const [msg, setMsg] = useState('');
  const [busy, setBusy] = useState(false);
  const [loadError, setLoadError] = useState('');
  const [newDocCategory, setNewDocCategory] = useState('financials');
  const [newDocVisibility, setNewDocVisibility] = useState('locked');
  useEffect(() => setTab(resolveTab(location.pathname)), [location.pathname]);
  async function load() { if (!profile) return; setBusy(true); setLoadError(''); try { const biz = await getMyBusiness(profile.id); setB(biz); if (biz) { const [{ data: f }, { data: im }, { data: req }, { data: int }] = await Promise.all([supabase.from('business_files').select('*').eq('business_id', biz.id).order('created_at', { ascending: false }), supabase.from('business_images').select('*').eq('business_id', biz.id).order('sort_order', { ascending: true, nullsFirst: false }).order('created_at', { ascending: true }), supabase.from('request_data').select('*, investors(code,title_en,title_vi,type,country,privacy)').eq('business_id', biz.id).order('created_at', { ascending: false }), supabase.from('investor_interests').select('*, investors(id,code,title_en,title_vi,type,country,industries,deal_types,ticket_min,ticket_max,privacy)').eq('business_id', biz.id).order('created_at', { ascending: false })]); setFiles(f || []); setImages(im || []); setRequests(req || []); setInterests(int || []); } } catch (e: any) { setLoadError(e?.message || 'Could not load dashboard data.'); } finally { setBusy(false); } }
  useEffect(() => { if (!loading && !profile) navigate('/login?next=/dashboard/business'); if (profile) load(); }, [profile?.id, loading]);
  if (loading) return <section style={{ maxWidth: 1240, margin: '0 auto', padding: '56px 24px' }}><div style={cardStyle}>Loading...</div></section>;
  if (!profile) return <Navigate to="/login?next=/dashboard/business" replace />;
  if (profile.role !== 'business' && profile.role !== 'admin') return <section style={{ maxWidth: 760, margin: '0 auto', padding: '56px 24px' }}><div style={cardStyle}><h2>Business access only</h2><p>Role hiện tại: {profile.role}</p><Link to="/" style={{ color: '#1596cc', fontWeight: 700 }}>Back home</Link></div></section>;
  if (!b) return <section style={{ maxWidth: 760, margin: '0 auto', padding: '56px 24px' }}><div style={{ ...cardStyle, textAlign: 'center' }}><h2>Business profile not found</h2><p style={{ color: '#64748B' }}>Tài khoản này chưa có hồ sơ DN hoặc đang chờ admin kích hoạt.</p><Link to="/register/business" style={{ display: 'inline-block', background: '#0F2A4A', color: '#fff', fontWeight: 700, padding: '12px 22px', borderRadius: 10 }}>Tạo hồ sơ DN</Link></div></section>;
  const score = b.quality_score === null || b.quality_score === undefined ? null : Math.round(Number(b.quality_score));
  const scoreValue = score ?? 0;
  const band = qBand(scoreValue);
  const quotaTotal = Number(b.quota_total || (String(b.plan || '').includes('featured') ? 200 : 100));
  const quotaUsed = Number(b.quota_used || 0);
  const quotaRemaining = Math.max(0, quotaTotal - quotaUsed);
  const quotaPct = Math.min(100, Math.round((quotaUsed / Math.max(1, quotaTotal)) * 100));
  const planLabel = String(b.plan || 'Standard');
  const statusLabel = String(b.status || 'Live');
  const title = b.company_name_private || b.title_vi || b.title_en || 'Business profile';
  async function saveProfile(e: FormEvent) { e.preventDefault(); const fd = new FormData(e.currentTarget as HTMLFormElement); const pending: any = { company_name_private: fd.get('company_name_private'), title_vi: fd.get('title_vi'), description_vi: fd.get('description_vi'), industry: fd.get('industry'), deal_type: fd.get('deal_type'), city: fd.get('city'), highlights_vi: fd.get('highlights_vi'), investment_reason_vi: fd.get('investment_reason_vi'), revenue_2025: Number(fd.get('revenue_2025') || 0), ebitda_margin: Number(fd.get('ebitda_margin') || 0), ask_amount: Number(fd.get('ask_amount') || 0), stake_pct: Number(fd.get('stake_pct') || 0), data_confidence: Number(fd.get('data_confidence') || 0) }; const { error } = await supabase.from('businesses').update({ pending_changes_json: pending, status: 'pending_admin_review' }).eq('id', b.id); setMsg(error ? error.message : T(lang, 'Đã lưu thay đổi, chờ Admin duyệt lại.', 'Saved changes, pending Admin re-review.')); await load(); }
  async function fileChange(e: any) { const file = e.target.files?.[0]; if (!file || !b || !profile) return; setBusy(true); try { await uploadBusinessFile(b.id, profile.id, file, newDocCategory, newDocVisibility, newDocName || file.name); setMsg('File uploaded successfully.'); await load(); } catch (err: any) { setMsg(err?.message || 'Upload failed.'); } finally { setBusy(false); e.target.value = ''; } }
  async function imageChange(e: any) { const file = e.target.files?.[0]; if (!file || !b || !profile) return; setBusy(true); try { await uploadBusinessImage(b.id, profile.id, file, file.name); setMsg('Image uploaded successfully. Ảnh sẽ chỉ hiện public sau khi Admin duyệt/làm mờ thông tin nhạy cảm.'); await load(); } catch (err: any) { setMsg(err?.message || 'Upload failed.'); } finally { setBusy(false); e.target.value = ''; } }
  async function deleteFile(row: any) { if (!confirm(T(lang, 'Xóa tài liệu này?', 'Delete this document?'))) return; try { await deleteBusinessFile(row); setMsg('Document deleted.'); await load(); } catch (e: any) { setMsg(e?.message || 'Delete failed.'); } }
  async function deleteImage(row: any) { if (!confirm(T(lang, 'Xóa ảnh này?', 'Delete this image?'))) return; try { await deleteBusinessImage(row); setMsg('Image deleted.'); await load(); } catch (e: any) { setMsg(e?.message || 'Delete failed.'); } }
  async function renameImage(row: any) { const title = prompt(T(lang, 'Tên ảnh mới', 'New image title'), row.title || ''); if (title === null) return; try { await updateBusinessImage(row.id, { title }); setMsg('Image title updated.'); await load(); } catch (e: any) { setMsg(e?.message || 'Update failed.'); } }
  async function suggestHero(row: any) { try { await updateBusinessImage(row.id, { admin_note: 'User suggests this as hero image' }); setMsg('Đã ghi nhận ảnh đề xuất. Admin sẽ chọn ảnh public/hero sau khi kiểm duyệt.'); await load(); } catch (e: any) { setMsg(e?.message || 'Update failed.'); } }
  async function acceptInterest(row: any) { const { error } = await supabase.from('investor_interests').update({ status: 'connected' }).eq('id', row.id); setMsg(error ? error.message : 'Đã đồng ý kết nối.'); load(); }
  async function rejectInterest(row: any) { const { error } = await supabase.from('investor_interests').update({ status: 'rejected' }).eq('id', row.id); setMsg(error ? error.message : 'Đã từ chối kết nối.'); load(); }
  async function fulfillRequest(row: any) { const { error } = await supabase.from('request_data').update({ status: 'fulfilled' }).eq('id', row.id); setMsg(error ? error.message : 'Marked as fulfilled.'); load(); }

  return <div style={{ width: '100%', overflowX: 'hidden', background: '#F7FAFC' }}><div style={{ maxWidth: 1240, margin: '0 auto', padding: '26px 24px 60px' }}><div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: 16, flexWrap: 'wrap', marginBottom: 22 }}><div><div style={{ fontSize: 12.5, fontWeight: 700, color: '#94A3B8', textTransform: 'uppercase', letterSpacing: .5, marginBottom: 4 }}>Business Dashboard</div><h1 style={{ fontSize: 25, fontWeight: 800, letterSpacing: -.5, margin: 0 }}>{title}</h1></div><div style={{ display: 'flex', gap: 8, alignItems: 'center', flexWrap: 'wrap' }}><button onClick={() => setLang(lang === 'vi' ? 'en' : 'vi')} style={{ border: '1px solid #E2E8F0', background: '#fff', borderRadius: 999, padding: '8px 14px', fontWeight: 700 }}>{lang.toUpperCase()}</button><span style={tag('#EAF0F6','#0F2A4A')}>{planLabel}</span><span style={tag('#E9F9EF','#16A34A')}>{statusLabel}</span><button onClick={() => signOut().then(() => navigate('/'))} style={{ background: '#EEF2F6', color: '#334155', fontWeight: 700, fontSize: 14, padding: '10px 16px', borderRadius: 9, border: 'none' }}>{T(lang, 'Thoát', 'Exit')}</button></div></div>
    <div className="d68-dash-cols" style={{ display: 'grid', gridTemplateColumns: '230px minmax(0,1fr)', gap: 24, alignItems: 'start' }}><nav className="d68-side-nav" style={{ position: 'sticky', top: 90, background: '#fff', border: '1px solid #E7EDF3', borderRadius: 16, padding: 10, display: 'flex', flexDirection: 'column', gap: 2 }}>{tabs.map((t) => <Link key={t.id} to={t.href} onClick={() => setTab(t.id)} style={{ textAlign: 'left', border: 'none', borderRadius: 10, padding: '11px 12px', fontSize: 13.5, fontWeight: tab === t.id ? 800 : 600, color: tab === t.id ? '#0F2A4A' : '#64748B', background: tab === t.id ? '#E7F6FD' : 'transparent' }}>{t.icon} {T(lang, t.vi, t.en)}</Link>)}<div style={{ borderTop: '1px solid #EEF2F6', marginTop: 6, paddingTop: 10 }}><Link to={`/businesses/${b.slug}`} style={{ display: 'block', textAlign: 'center', fontSize: 13, fontWeight: 700, color: '#1596cc', padding: 10 }}>{T(lang, 'Xem trang public', 'View public page')} ↗</Link></div></nav><main>{b.pending_changes_json ? <Notice text={T(lang, 'Hồ sơ đang chờ Admin duyệt lại. Public page sẽ dùng dữ liệu đã duyệt gần nhất.', 'Profile is pending Admin re-review. Public page uses latest approved data.')} /> : null}{loadError ? <Notice text={loadError} warn /> : null}{msg ? <Notice text={msg} /> : null}{busy ? <Notice text={T(lang, 'Đang xử lý dữ liệu...', 'Processing data...')} /> : null}
      {tab === 'overview' ? <><div style={{ ...cardStyle, marginBottom: 22, display: 'flex', alignItems: 'center', gap: 24, flexWrap: 'wrap' }}><div style={{ flexShrink: 0, width: 120, height: 120, borderRadius: '50%', background: `conic-gradient(${band.color} ${Math.max(0, Math.min(100, scoreValue)) * 3.6}deg, #EEF2F6 0deg)`, display: 'flex', alignItems: 'center', justifyContent: 'center' }}><div style={{ width: 92, height: 92, borderRadius: '50%', background: '#fff', display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}><span style={{ fontSize: 29, fontWeight: 800, color: band.color }}>{score === null ? '—' : score}</span><span style={{ fontSize: 10, fontWeight: 700, color: '#94A3B8' }}>/ 100</span></div></div><div style={{ flex: 1, minWidth: 250 }}><div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 6 }}><span style={{ fontSize: 15.5, fontWeight: 800 }}>Business Quality Score</span><span style={{ fontSize: 12, fontWeight: 800, padding: '6px 10px', borderRadius: 8, color: band.color, background: band.bg }}>{score === null ? T(lang, 'Đang cập nhật', 'Pending') : T(lang, band.labelVi, band.labelEn)}</span></div><p style={{ fontSize: 13, color: '#64748B', lineHeight: 1.55, margin: 0 }}>{T(lang, 'Điểm lấy từ quality_score thực tế trong database, không dùng điểm mặc định.', 'Score comes from actual database quality_score, no default score is used.')}</p></div></div><div className="d68-cards-4" style={{ display: 'grid', gridTemplateColumns: 'repeat(4,1fr)', gap: 16, marginBottom: 22 }}>{metricCard(T(lang,'Trạng thái hồ sơ','Profile status'), statusLabel, '#16A34A')}{metricCard(T(lang,'Gói hiển thị','Plan tier'), planLabel)}{metricCard(T(lang,'Ảnh / File','Images / Files'), `${images.length} / ${files.length}`)}{metricCard(T(lang,'Lượt xem hồ sơ','Profile views'), String(Number(b.views || 0)))}</div><div style={{ background: '#0F2A4A', color: '#fff', borderRadius: 16, padding: 24, marginBottom: 22 }}><div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 12 }}><span style={{ fontSize: 14, fontWeight: 700, color: '#c6d5e6' }}>{T(lang,'Hạn mức gửi Proposal','Proposal quota')}</span><span style={{ fontSize: 18, fontWeight: 800, color: '#F2B51D' }}>{quotaUsed} / {quotaTotal}</span></div><div style={{ height: 10, borderRadius: 999, background: 'rgba(255,255,255,.12)', overflow: 'hidden' }}><div style={{ height: '100%', width: `${quotaPct}%`, background: 'linear-gradient(90deg,#1BADEA,#F2B51D)', borderRadius: 999 }} /></div><p style={{ fontSize: 12.5, color: '#9db4cc', margin: '12px 0 0' }}>{T(lang, `Còn ${quotaRemaining}/${quotaTotal} proposal.`, `${quotaRemaining}/${quotaTotal} proposals remaining.`)} <Link to="/investors" style={{ color: '#F2B51D', fontWeight: 700 }}>{T(lang,'Tìm Nhà đầu tư','Find investors')} →</Link></p></div></> : null}
      {tab === 'profile' || tab === 'datacenter' ? <ProfileForm lang={lang} b={b} saveProfile={saveProfile} /> : null}
      {tab === 'documents' ? <Documents lang={lang} files={files} deleteFile={deleteFile} fileChange={fileChange} newDocName={newDocName} setNewDocName={setNewDocName} newDocCategory={newDocCategory} setNewDocCategory={setNewDocCategory} newDocVisibility={newDocVisibility} setNewDocVisibility={setNewDocVisibility} /> : null}
      {tab === 'images' ? <Images lang={lang} images={images} imageChange={imageChange} deleteImage={deleteImage} renameImage={renameImage} suggestHero={suggestHero} /> : null}
      {tab === 'interests' ? <SimpleRows title={T(lang,'Nhà đầu tư quan tâm','Investor interests')} rows={interests} empty={T(lang,'Chưa có nhà đầu tư quan tâm.','No investor interests yet.')} actions={(row: any) => <><button onClick={() => acceptInterest(row)} style={smallBtn('#16A34A')}>Accept</button><button onClick={() => rejectInterest(row)} style={smallBtn('#DC2626')}>Reject</button></>} /> : null}
      {tab === 'requests' ? <SimpleRows title={T(lang,'Yêu cầu dữ liệu','Data requests')} rows={requests} empty={T(lang,'Chưa có yêu cầu dữ liệu.','No data requests yet.')} actions={(row: any) => <button onClick={() => fulfillRequest(row)} style={smallBtn('#16A34A')}>Fulfilled</button>} /> : null}
      {tab === 'services' ? <div style={cardStyle}><h2>Services & Billing</h2><p style={{ color: '#64748B' }}>Plan: {planLabel}. Status: {statusLabel}.</p><Link to="/pricing" style={{ color: '#1596cc', fontWeight: 800 }}>Renew / Upgrade →</Link></div> : null}
    </main></div></div></div>;
}
function ProfileForm({ lang, b, saveProfile }: any) { return <form onSubmit={saveProfile} style={{ ...cardStyle, display: 'flex', flexDirection: 'column', gap: 16 }}><h2 style={{ fontSize: 19, fontWeight: 800, margin: '0 0 6px' }}>{T(lang, 'Chỉnh sửa hồ sơ', 'Edit profile')}</h2><p style={{ fontSize: 13, color: '#64748B', margin: 0, lineHeight: 1.55 }}>{T(lang, 'Cập nhật dữ liệu gốc. Các field EN do Admin/chuyên viên cập nhật riêng để tránh dịch máy sai.', 'Update source data. EN fields are curated separately by Admin/specialist to avoid bad machine translation.')}</p><label style={labelStyle}><span style={labelText}>{T(lang,'Tên doanh nghiệp (thật — chỉ Admin thấy)','Business name (real — Admin only)')}</span><input name="company_name_private" defaultValue={b.company_name_private || ''} style={inputStyle} /></label><label style={labelStyle}><span style={labelText}>{T(lang,'Tiêu đề ẩn danh public','Anonymous public title')}</span><input name="title_vi" defaultValue={b.title_vi || ''} style={inputStyle} /></label><div className="d68-form-2" style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16 }}><label style={labelStyle}><span style={labelText}>{T(lang,'Ngành','Industry')}</span><input name="industry" defaultValue={b.industry || ''} style={inputStyle} /></label><label style={labelStyle}><span style={labelText}>{T(lang,'Thành phố','City')}</span><input name="city" defaultValue={b.city || ''} style={inputStyle} /></label></div><label style={labelStyle}><span style={labelText}>{T(lang,'Tổng quan doanh nghiệp','Business overview')}</span><textarea name="description_vi" defaultValue={b.description_vi || ''} rows={3} style={{ ...inputStyle, resize: 'vertical' }} /></label><label style={labelStyle}><span style={labelText}>{T(lang,'Điểm nổi bật','Highlights')}</span><textarea name="highlights_vi" defaultValue={b.highlights_vi || ''} rows={4} style={{ ...inputStyle, resize: 'vertical' }} /></label><div style={{ borderTop: '1px solid #EEF2F6', paddingTop: 16 }}><div className="d68-form-2" style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16 }}><label style={labelStyle}><span style={labelText}>{T(lang,'Phân khúc giao dịch','Deal segment')}</span><input name="deal_type" defaultValue={b.deal_type || ''} style={inputStyle} /></label><label style={labelStyle}><span style={labelText}>{T(lang,'Doanh thu năm gần nhất','Latest annual revenue')}</span><input name="revenue_2025" type="number" defaultValue={b.revenue_2025 || 0} style={inputStyle} /></label><label style={labelStyle}><span style={labelText}>EBITDA margin (%)</span><input name="ebitda_margin" type="number" defaultValue={b.ebitda_margin || 0} style={inputStyle} /></label><label style={labelStyle}><span style={labelText}>{T(lang,'Độ tin cậy dữ liệu','Data confidence')}</span><input name="data_confidence" type="number" defaultValue={b.data_confidence || 0} style={inputStyle} /></label><label style={labelStyle}><span style={labelText}>{T(lang,'Tỷ lệ cổ phần (%)','Stake (%)')}</span><input name="stake_pct" type="number" defaultValue={b.stake_pct || 0} style={inputStyle} /></label><label style={labelStyle}><span style={labelText}>{T(lang,'Số tiền gọi vốn / giá trị giao dịch','Amount sought / asking')}</span><input name="ask_amount" type="number" defaultValue={b.ask_amount || 0} style={inputStyle} /></label></div><label style={{ ...labelStyle, marginTop: 16 }}><span style={labelText}>{T(lang,'Lý do gọi vốn / bán / cách dùng tiền','Reason for raise / sale / use of funds')}</span><textarea name="investment_reason_vi" defaultValue={b.investment_reason_vi || ''} rows={3} style={{ ...inputStyle, resize: 'vertical' }} /></label></div><div style={{ display: 'flex', justifyContent: 'flex-end' }}><button type="submit" style={smallBtn('#0F2A4A')}>{T(lang,'Lưu thay đổi','Save changes')}</button></div></form>; }
function Documents({ lang, files, deleteFile, fileChange, newDocName, setNewDocName, newDocCategory, setNewDocCategory, newDocVisibility, setNewDocVisibility }: any) { return <div style={cardStyle}><h2>{T(lang,'Tài liệu doanh nghiệp','Business documents')}</h2><p style={{ color: '#64748B' }}>{T(lang,'Hiển thị tên file và định dạng thực tế từ database/storage.', 'Shows actual file names and formats from database/storage.')}</p><div>{files.map((d: any) => <div key={d.id} style={{ display: 'flex', alignItems: 'center', gap: 14, padding: '14px 0', borderBottom: '1px solid #F1F5F9' }}><span style={{ width: 42, height: 42, borderRadius: 10, background: '#E9F9EF', color: '#16A34A', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>📄</span><div style={{ flex: 1 }}><div style={{ fontSize: 14.5, fontWeight: 700 }}>{d.display_name || d.file_name || d.filename || d.name}</div><div style={{ fontSize: 12, color: '#94A3B8' }}>{d.category || 'document'} · {d.privacy_level || d.visibility || 'locked'} · {ext(d.file_name || d.filename || d.name, d.file_type)}</div></div><button onClick={() => deleteFile(d)} style={smallBtn('#DC2626')}>{T(lang,'Xóa','Delete')}</button></div>)}{!files.length ? <div style={{ color: '#94A3B8', padding: '20px 0' }}>{T(lang,'Chưa có tài liệu.','No documents yet.')}</div> : null}</div><div style={{ marginTop: 22, paddingTop: 20, borderTop: '1px solid #EEF2F6', display: 'flex', gap: 10, flexWrap: 'wrap', alignItems: 'flex-end' }}><label style={labelStyle}><span style={labelText}>{T(lang,'Tên hiển thị đề xuất','Suggested display name')}</span><input value={newDocName} onChange={(e) => setNewDocName(e.target.value)} placeholder={T(lang,'Báo cáo tài chính 2025','Financial report 2025')} style={inputStyle} /></label><label style={labelStyle}><span style={labelText}>{T(lang,'Danh mục','Category')}</span><select value={newDocCategory} onChange={(e) => setNewDocCategory(e.target.value)} style={inputStyle}><option value="financials">Financials</option><option value="profile">Profile</option><option value="im">Teaser / IM</option><option value="legal">Legal</option><option value="other">Other</option></select></label><label style={labelStyle}><span style={labelText}>{T(lang,'Hiển thị','Visibility')}</span><select value={newDocVisibility} onChange={(e) => setNewDocVisibility(e.target.value)} style={inputStyle}><option value="locked">🔒 Locked</option><option value="public">🌐 Public</option></select></label><label style={{ background: '#0F2A4A', color: '#fff', fontWeight: 700, fontSize: 13.5, padding: '12px 20px', borderRadius: 9, cursor: 'pointer' }}>+ {T(lang,'Tải lên','Upload')}<input type="file" onChange={fileChange} style={{ display: 'none' }} /></label></div></div>; }
function Images({ lang, images, imageChange, deleteImage, renameImage, suggestHero }: any) { return <div style={cardStyle}><h2>{T(lang,'Ảnh doanh nghiệp','Business images')}</h2><p style={{ color: '#64748B' }}>{T(lang,'DN có thể thêm/đổi tên/xóa ảnh. Ảnh chỉ hiển thị public sau khi Admin kiểm duyệt, làm mờ logo/tên DN và bật public.', 'Business can add/rename/delete images. Images are public only after Admin review, sanitization and approval.')}</p><div className="d68-form-2" style={{ display: 'grid', gridTemplateColumns: 'repeat(3,1fr)', gap: 14 }}>{images.map((img: any) => <div key={img.id} style={{ border: '1px solid #E2E8F0', borderRadius: 12, padding: 10, background: '#fff' }}><div style={{ height: 150, borderRadius: 10, overflow: 'hidden', background: '#F7FAFC' }}><img src={img.public_url || img.url} alt={img.title || ''} style={{ width: '100%', height: '100%', objectFit: 'cover' }} /></div><div style={{ fontSize: 13, fontWeight: 700, margin: '8px 0' }}>{img.title || T(lang,'Chưa đặt tên','Untitled')}</div><div style={{ display: 'flex', flexWrap: 'wrap', gap: 6 }}><button onClick={() => renameImage(img)} style={tinyBtn}>{T(lang,'Đổi tên','Rename')}</button><button onClick={() => suggestHero(img)} style={tinyBtn}>{T(lang,'Đề xuất hero','Suggest hero')}</button><button onClick={() => deleteImage(img)} style={{ ...tinyBtn, color: '#DC2626', borderColor: '#FCA5A5' }}>{T(lang,'Xóa','Delete')}</button></div></div>)}<label style={{ minHeight: 210, border: '1px dashed #CBD5E1', borderRadius: 12, background: '#F7FAFC', display: 'flex', alignItems: 'center', justifyContent: 'center', color: '#64748B', cursor: 'pointer', textAlign: 'center', padding: 16 }}>+ {T(lang,'Thêm ảnh','Add image')}<input type="file" accept="image/*" onChange={imageChange} style={{ display: 'none' }} /></label></div></div>; }
function SimpleRows({ title, rows, empty, actions }: any) { return <div style={cardStyle}><h2>{title}</h2>{rows.length ? rows.map((r: any) => <div key={r.id} style={{ display: 'flex', justifyContent: 'space-between', gap: 12, alignItems: 'center', borderTop: '1px solid #F1F5F9', padding: '12px 0' }}><div><b>{r.investors?.title_vi || r.investors?.title_en || r.investors?.code || r.status}</b><div style={{ color: '#94A3B8', fontSize: 12 }}>{r.status}</div></div><div style={{ display: 'flex', gap: 8 }}>{actions(r)}</div></div>) : <p style={{ color: '#94A3B8' }}>{empty}</p>}</div>; }
function Notice({ text, warn=false }: { text: string; warn?: boolean }) { return <div style={{ ...cardStyle, marginBottom: 16, color: warn ? '#B91C1C' : '#16A34A', fontWeight: 700 }}>{text}</div>; }
function tag(bg: string, color: string) { return { fontSize: 12, fontWeight: 800, padding: '6px 12px', borderRadius: 8, background: bg, color } as const; }
function smallBtn(color = '#0F2A4A') { return { background: color, color: '#fff', fontWeight: 700, fontSize: 13, padding: '9px 13px', borderRadius: 9, border: 'none', cursor: 'pointer' } as const; }
const tinyBtn: CSSProperties = { border: '1px solid #E2E8F0', background: '#fff', borderRadius: 7, padding: '6px 9px', fontSize: 12, fontWeight: 700, cursor: 'pointer', color: '#334155' };
