import { useEffect, useState } from 'react';
import { useSearchParams } from 'react-router-dom';
import InvestorCard from '../components/InvestorCard';
import { listInvestors } from '../lib/data';
import type { Lang } from '../lib/i18n';
export default function Investors({ lang }: { lang: Lang }){ const [params]=useSearchParams(); const [filters,setFilters]=useState({search:params.get('q')||'',country:params.get('country')||'',industry:params.get('industry')||'',type:''}); const [items,setItems]=useState<any[]>([]); async function load(){setItems(await listInvestors(filters).catch(()=>[]));} useEffect(()=>{load()},[]); return <section className="section"><div className="container"><div className="section-title"><div><h2>{lang==='en'?'Investors on Deals68':'Nhà đầu tư trên Deals68'}</h2><p className="muted">{items.length} anonymous public investor profiles</p></div></div><div className="filters"><input className="input" placeholder="Search" value={filters.search} onChange={e=>setFilters({...filters,search:e.target.value})}/><select className="select" value={filters.type} onChange={e=>setFilters({...filters,type:e.target.value})}><option value="">All types</option><option>VC</option><option>PE</option><option>Corporate/Strategic</option><option>Individual/Angel</option><option>Lender/Debt</option></select><select className="select" value={filters.country} onChange={e=>setFilters({...filters,country:e.target.value})}><option value="">All countries</option><option value="VN">Vietnam</option><option value="SG">Singapore</option><option value="US">USA</option><option value="JP">Japan</option><option value="KR">Korea</option></select><button className="btn" onClick={load}>Filter</button></div><div className="grid">{items.map(i=><InvestorCard key={i.id || i.code} inv={i} lang={lang==='vi'?'vi':'en'}/>)}</div></div></section> }
