name: Apply valuation and business location v1

on:
  pull_request:
    branches:
      - building
    types:
      - opened
      - reopened
      - synchronize

permissions:
  contents: write

jobs:
  apply:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - name: Checkout building
        uses: actions/checkout@v4
        with:
          ref: building
          fetch-depth: 1
          show-progress: false

      - name: Apply scoped patch
        run: python3 scripts/apply-valuation-business-location-v1.py

      - name: Install dependencies
        run: npm ci

      - name: Build
        id: build
        continue-on-error: true
        shell: bash
        run: npm run build > /tmp/valuation-business-location-build.log 2>&1

      - name: Upload build diagnostic
        if: steps.build.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: valuation-business-location-build-diagnostic
          path: /tmp/valuation-business-location-build.log
          retention-days: 1

      - name: Stop after build failure
        if: steps.build.outcome == 'failure'
        shell: bash
        run: |
          cat /tmp/valuation-business-location-build.log
          exit 1

      - name: Commit to building and remove one-time files
        if: steps.build.outcome == 'success'
        shell: bash
        run: |
          rm -f scripts/apply-valuation-business-location-v1.py
          rm -f .github/workflows/apply-valuation-business-location-v1.yml
          rm -f docs/.trigger-valuation-business-location-v1
          rm -f docs/.trigger-valuation-business-location-v1-run
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(valuation): align inputs and normalize business location facets"
          git push origin HEAD:building
