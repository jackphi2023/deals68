name: Investor Profile V10 QA

on:
  push:
    branches:
      - feature/investor-profile-v10
      - beta-reference
  pull_request:
    branches:
      - beta-reference
    paths:
      - 'netlify.toml'
      - 'public/assets/investor-cover-default.svg'
      - 'src/App.tsx'
      - 'src/components/admin/**'
      - 'src/components/investor/**'
      - 'src/lib/investorAdminV10.ts'
      - 'src/lib/investorProfileService.ts'
      - 'src/pages/AdminBannersV10.tsx'
      - 'src/pages/AdminInvestorsV10.tsx'
      - 'src/pages/InvestorDetailV10.tsx'
      - 'src/pages/InvestorProfileV10.tsx'
      - 'src/pages/Investors.tsx'
      - 'src/styles/index.css'
      - 'src/styles/pages/investor-profile-v10.css'
      - 'src/styles/pages/entity-ui-v12.css'
      - 'scripts/deals68-investor-profile-v10-check.mjs'
      - 'scripts/deals68-investor-list-v12-check.mjs'
      - 'scripts/deals68-investor-v10-beta-smoke.mjs'
      - 'scripts/deals68-entity-ui-v12-beta-smoke.mjs'
      - 'scripts/deals68-package-checks.mjs'
      - 'scripts/deals68-migration-state-check.mjs'
      - 'supabase/migrations/20260715045336_investor_profile_cover_appetite_v1.sql'
      - 'supabase/migrations/20260715071812_investor_profile_cover_appetite_v2.sql'
      - 'supabase/migrations/20260715085429_investor_profile_cover_appetite_v3_privilege_hardening.sql'
      - '.github/workflows/investor-profile-v10-qa.yml'

permissions:
  contents: read
  statuses: write

jobs:
  build-and-package-qa:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: TypeScript and Vite production build
        env:
          VITE_SUPABASE_URL: https://local-test.supabase.co
          VITE_SUPABASE_ANON_KEY: local-test-anon-key
        run: npm run build

      - name: Investor Profile V12 semantic QA
        run: node scripts/deals68-investor-profile-v10-check.mjs

      - name: Investor List V12 scoped metadata QA
        run: node scripts/deals68-investor-list-v12-check.mjs

      - name: Beta smoke parser QA
        run: |
          node --check scripts/deals68-investor-v10-beta-smoke.mjs
          node --check scripts/deals68-entity-ui-v12-beta-smoke.mjs

      - name: Font CSP contract QA
        run: |
          grep -F "style-src 'self' 'unsafe-inline' https://fonts.googleapis.com" netlify.toml
          grep -F "font-src 'self' data: https://fonts.gstatic.com" netlify.toml

      - name: Full package QA
        run: npm run qa:packages

      - name: CSS architecture QA
        run: npm run qa:css

      - name: Migration state QA
        run: npm run qa:migrations

  beta-public-smoke:
    if: github.event_name == 'push' && github.ref == 'refs/heads/beta-reference'
    needs: build-and-package-qa
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Publish pending Beta smoke status
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api --method POST \
            "repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" \
            -f state=pending \
            -f context='deals68/investor-v10-beta-smoke' \
            -f description='Waiting for exact Netlify Beta Investor V11 and Entity UI V12 deployment' \
            -f target_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"

      - name: Checkout deployed Beta commit
        uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Install Playwright Chromium and Linux dependencies
        run: npx playwright install --with-deps chromium

      - name: Resolve Netlify Beta and run real Investor V11 smoke
        env:
          D68_BETA_URLS: https://beta-reference-deals68.netlify.app,https://beta-reference--deals68.netlify.app
          D68_RELEASE_SHA: ${{ github.sha }}
        run: node scripts/deals68-investor-v10-beta-smoke.mjs

      - name: Run real Entity UI V12 Beta smoke
        env:
          D68_BETA_URLS: https://beta-reference-deals68.netlify.app,https://beta-reference--deals68.netlify.app
          D68_RELEASE_SHA: ${{ github.sha }}
        run: node scripts/deals68-entity-ui-v12-beta-smoke.mjs

      - name: Publish successful Beta smoke status
        if: success()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api --method POST \
            "repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" \
            -f state=success \
            -f context='deals68/investor-v10-beta-smoke' \
            -f description='Real Netlify Beta Investor V11 and Entity UI V12 smokes passed' \
            -f target_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"

      - name: Publish failed Beta smoke status
        if: failure()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api --method POST \
            "repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" \
            -f state=failure \
            -f context='deals68/investor-v10-beta-smoke' \
            -f description='Real Netlify Beta Investor or Entity UI smoke failed; inspect diagnostics' \
            -f target_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"

      - name: Upload Beta smoke diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: investor-v12-beta-smoke-${{ github.sha }}
          path: |
            /tmp/deals68-investor-v11-beta-desktop.png
            /tmp/deals68-investor-v11-beta-tablet.png
            /tmp/deals68-investor-v11-beta-mobile.png
            /tmp/deals68-investor-v11-beta-failure.png
            /tmp/deals68-investor-v11-beta-diagnostic.json
            /tmp/deals68-entity-ui-v12-home.png
            /tmp/deals68-entity-ui-v12-listings.png
            /tmp/deals68-entity-ui-v12-detail-desktop.png
            /tmp/deals68-entity-ui-v12-detail-mobile.png
          if-no-files-found: ignore
          retention-days: 7
