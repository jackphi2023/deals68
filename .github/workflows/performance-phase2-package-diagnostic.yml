name: Capture performance phase 2 package QA

on:
  pull_request:
    branches: [building]
    types: [synchronize]

permissions:
  contents: read

jobs:
  diagnose-packages:
    if: github.event.pull_request.head.ref == 'automation/performance-phase2-route-split-20260721'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: automation/performance-phase2-route-split-20260721
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Run and capture package QA
        shell: bash
        run: |
          set +e
          npm run qa:packages > /tmp/phase2-package-qa.log 2>&1
          status=$?
          set -e
          cat /tmp/phase2-package-qa.log
          echo "$status" > /tmp/phase2-package-qa-status.txt
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: performance-phase2-package-qa-log
          path: |
            /tmp/phase2-package-qa.log
            /tmp/phase2-package-qa-status.txt
          retention-days: 7
